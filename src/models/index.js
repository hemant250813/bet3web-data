const {
  User,
  UserLoginHistory,
  BankAccount,
  ResultTransaction,
} = require("./user");
const Otp = require("./otp");
const Transaction = require("./transaction");

module.exports = {
  User,
  UserLoginHistory,
  BankAccount,
  ResultTransaction,
  Otp,
  Transaction
};
